// Create a global for football types and interfaces
declare global {
    interface Sport {
        id: number;
        name: string;
        description: string;
        isActive: boolean;
    }
}
export { };


